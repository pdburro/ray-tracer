#include <iostream>

int main() {
    // Checkpoint 02: write a PPM header and gradient pixels to std::cout.
    // Keep diagnostics on std::clog so future image output stays valid.
    std::clog << "Ray tracer starter ready. Next: output your first PPM image.\n";
    return 0;
}
